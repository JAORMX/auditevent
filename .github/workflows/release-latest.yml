name: Release latest

on:
  push:
    branches:
      - main

jobs:
  # Push to latest
  container-push-latest:
    uses: metal-toolbox/container-push/.github/workflows/container-push.yml@df8644f53f5065cdd501600a6e052a7472732f6a # main
    with:
      name: audittail
      tag: latest
      dockerfile_path: images/audittail/Dockerfile
      platforms: linux/amd64,linux/arm64
